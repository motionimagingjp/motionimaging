{
  "name": "partyconnect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "街コン運営WEBアプリ PartyConnect",
  "scripts": {
    "test": "npm run test:unit && npm run test:functions && npm run test:sql",
    "test:unit": "node --test tests/*.test.ts",
    "test:functions": "bash scripts/check-functions.sh",
    "test:sql": "bash scripts/test-sql.sh"
  }
}
